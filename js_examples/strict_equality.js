// = assignment operator
// == comparison operator
// === strict equality oeprator, compares value and datatype
// != inequality operator
// !== strict inequality operator

const PI = 3.14;

if(PI !== "3.14"){
    console.log("That is pi");
}
else{
    console.log("That is NOT pi")
}